@* Stub: клиентская валидация в проекте не используется (нет wwwroot/lib/jquery-validation*).
   Файл нужен только чтобы `<partial name="_ValidationScriptsPartial" />` находил view
   и не падал с InvalidOperationException. Серверная валидация через ModelState работает как обычно.
   Понадобятся jquery-unobtrusive скрипты — заменить содержимое на их подключение. *@
